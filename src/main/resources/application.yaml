spring:
  application:
    name: file-repository


  servlet:
    multipart:
      max-file-size: 10GB
      max-request-size: 10GB

filerepository:
  root:
    path: "${LOCAL_STORAGE_PATH:storage-files}"